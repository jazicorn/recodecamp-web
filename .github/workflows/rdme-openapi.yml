# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-07-28T19:00:12.659Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `dev`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - dev

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi apps/api/src/docs/apidoc.yaml --key=${{ secrets.README_API_KEY }} --id=64c3f7b4317eab000c23ee26        
